<!doctype html>
<html lang="da">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ page_title }} – {{ shop.name }}</title>
    {{ content_for_header }}
  </head>
  <body>
    <div style="position:fixed;bottom:8px;right:8px;background:#eee;padding:6px 10px;font:12px/1.2 system-ui;z-index:9999;">
      theme.liquid loaded
    </div>
    <main>
      {{ content_for_layout }}
    </main>
  </body>
</html>
